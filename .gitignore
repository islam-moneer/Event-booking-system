# Private working docs (business plan, backlog, implementation plan).
# Published documentation lives in docs/ and IS committed.
.planning/

# Agent tooling — local only, regenerable
.claude/
CLAUDE.md
.qwen/
skills-lock.json

# Node
node_modules/
coverage/
*.log
npm-debug.log*

# Env / secrets
.env
.env.test
.env.local
